let ValorProduto = parseFloat(prompt("Digite um valor para o Produto: "));

let reajuste = ValorProduto * 0.3333;

let somaDeValores = ValorProduto + reajuste;

alert("O Valor do reajuste é : " + reajuste);

alert("O valor final com o reajuste é : " + somaDeValores);
